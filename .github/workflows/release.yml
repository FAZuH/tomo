# Credit: Workflow configs based on https://github.com/Wynntils/Wynntils

name: Release & Deploy

on:
  push:
    branches:
      - release

permissions:
  contents: write
  packages: write

jobs:
  changelog:
    name: Generate Changelog
    runs-on: ubuntu-latest
    outputs:
      tag: ${{ steps.changelog.outputs.tag }}
      skipped: ${{ steps.changelog.outputs.skipped }}
      changelog: ${{ steps.changelog.outputs.changelog }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          fetch-tags: true

      - name: Fetch All Tags
        run: git fetch --tags --force origin '+refs/tags/*:refs/tags/*'

      - name: Set Up version.json
        run: |
          LATEST_TAG=$(git tag -l 'v*' --sort=-version:refname | head -1)
          echo "{\"version\":\"${LATEST_TAG:-v0.0.0}\"}" > version.json

      - name: Install changelog dependencies
        run: npm install conventional-changelog-conventionalcommits@7.0.2

      - name: Generate Changelog (dry run)
        id: changelog
        uses: TriPSs/conventional-changelog-action@v6
        with:
          skip-commit: true
          skip-tag: true
          github-token: ${{ secrets.PRIVATE_TOKEN }}
          config-file-path: ./.github/.config.js
          version-file: ./version.json
          preset: conventionalcommits
          skip-version-file: true
          skip-git-pull: true
          skip-on-empty: false

  patch-version:
    name: Patch Version
    needs: changelog
    if: ${{ needs.changelog.outputs.skipped != 'true' }}
    uses: ./.github/workflows/patch-version.yml
    with:
      version: ${{ needs.changelog.outputs.tag }}

  build:
    name: Build Binaries
    needs: [changelog, patch-version]
    if: ${{ needs.changelog.outputs.skipped != 'true' }}
    uses: ./.github/workflows/build.yml
    with:
      cargo-build-args: '--release --all-features --all-targets'
      upload-binary: true
      release: true
      require-patched-cargo: true

  publish-changelog:
    name: Publish Changelog Commit
    needs: [changelog, build]
    if: ${{ needs.changelog.outputs.skipped != 'true' }}
    outputs:
      tag: ${{ steps.commit.outputs.tag }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          fetch-tags: true

      - name: Fetch All Tags
        run: git fetch --tags --force origin '+refs/tags/*:refs/tags/*'

      - name: Set Up version.json
        run: |
          LATEST_TAG=$(git tag -l 'v*' --sort=-version:refname | head -1)
          echo "{\"version\":\"${LATEST_TAG:-v0.0.0}\"}" > version.json

      - name: Download patched Cargo.toml
        uses: actions/download-artifact@v4
        with:
          name: patched-cargo-toml
          path: .

      - name: Install changelog dependencies
        run: npm install conventional-changelog-conventionalcommits@7.0.2

      - name: Commit, Tag, Push
        id: commit
        uses: TriPSs/conventional-changelog-action@v6
        with:
          github-token: ${{ secrets.PRIVATE_TOKEN }}
          git-user-name: "faz-bot"
          git-user-email: "mail@fazuh.com"
          config-file-path: ./.github/.config.js
          version-file: ./version.json
          preset: conventionalcommits
          skip-version-file: true
          skip-git-pull: true
          skip-on-empty: false

  release-github:
    name: Release to GitHub
    needs: [changelog, build, publish-changelog]
    if: ${{ needs.changelog.outputs.skipped != 'true' }}
    runs-on: ubuntu-latest
    steps:
      - name: Download Artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts
          pattern: tomo-*

      - name: Prepare Release Files
        run: |
          mkdir -p release
          cp artifacts/tomo-linux-x64/tomo-linux-x64 release/
          cp artifacts/tomo-windows-x64.exe/tomo-windows-x64.exe release/
          cp artifacts/tomo-macos-x64/tomo-macos-x64 release/
          cp artifacts/tomo-macos-arm64/tomo-macos-arm64 release/

      - name: Create Release
        id: release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ needs.publish-changelog.outputs.tag }}
          body: ${{ needs.changelog.outputs.changelog }}
          files: release/*

      - name: Post Release on Discord
        uses: tsickert/discord-webhook@v6.0.0
        with:
          webhook-url: ${{ secrets.DISCORD_RELEASE_WEBHOOK_URL }}
          embed-color: "9498256"
          embed-title: "tomo ${{ needs.publish-changelog.outputs.tag }}"
          embed-description: ${{ needs.changelog.outputs.changelog }}
          embed-url: ${{ steps.release.outputs.url }}
          embed-timestamp: ${{ github.event.head_commit.timestamp }}
