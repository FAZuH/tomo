## Timer

```
fill(1)
length(3): state
length(2)
length(9): timer
length(1): progress_bar
length(1)
length(1): stats
length(1)
length(1): shortcuts
fill(1)
```

## Settings

```
length(): title

length(): timer_header
    length(): timer_time
    length(): timer_interval
    length(): timer_autostart

length(): hook_header
    length(): focus
    length(): short
    length(): long

length(): sound_header
    length(): focus
    length(): short
    length(): long
```
